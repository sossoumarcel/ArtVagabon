
export interface CardSkeletonProps {
  /**
   * Définit la largeur de la carte squelette.
   */
  width?: string | number;
  /**
   * Définit la hauteur de la carte squelette.
   */
  height?: string | number;
  /**
   * Définit la hauteur de l'emplacement de l'image.
   */
  imageHeight?: string | number;
}
